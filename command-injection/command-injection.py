import os

filename = input("Please provide a file name to search and display:\n")

command = "cat " + filename
if filname == os.path.basename(_file_):
    print("filename cannot be the current running file")
	os._exit(0)
os.system(command)
